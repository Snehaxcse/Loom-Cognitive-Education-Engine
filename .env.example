# Local-only fallback key for development (optional and not recommended for production)
# Must be a Groq key that starts with gsk_
VITE_GROQ_API_KEY=GROQ_API_KEY

# Preferred: use a backend route that reads GROQ_API_KEY server-side.
VITE_AI_API_URL=/api/generate
